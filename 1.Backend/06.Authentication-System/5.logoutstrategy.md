### 1. logout authentication in statfull
- logout click -> session id delete -> new request -> but session doesn't exist
- login -> session created -> user work -> logout -> session deleted from db -> access delete

### 2. logout in jwt authentication(stateless):
- 
10:00

Login

↓

Access Token (15 min)

Refresh Token (7 days)

↓

10:05

Logout

↓

Delete Access Token

↓

Delete Refresh Token

↓

10:15

Old Access Token expires

↓

No Refresh Token

↓

Must Login Again


- user clicks the token -> browser simply deleted that token or clear the cookie
- localStorage.removeItem("token");

- now client has no token - no token with reqest sent - 401 unathorized error -> needs to login again


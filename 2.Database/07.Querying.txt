- finding document in mongodb based on some conditions
- database -> apply condition -> return output based on condition

const product=await product.find(
    {

         brand: "apple"
    }
)

1. filtering
2. projection: retrive only specific field
User.find({
    "name email"
})